# Sutra for IntelliJ / WebStorm

From a clone:

```bash
cd extensions/intellij
gradle buildPlugin
```

Install: **Settings → Plugins → gear → Install Plugin from Disk…** and pick the zip under `build/distributions/`.

Or download the zip from GitHub Actions / Releases.
